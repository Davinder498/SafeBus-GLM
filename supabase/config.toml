# SafeBus has no approved Supabase Edge Functions. The current driver tracking
# path uses authenticated, authorization-reviewed Postgres RPCs. Keep the two
# retired prototype names disabled so a broad `supabase functions deploy`
# cannot publish them if obsolete source is accidentally restored.

[functions.ingest-location]
enabled = false

[functions.gps-stale-check]
enabled = false
